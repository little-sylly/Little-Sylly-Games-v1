# Fix Plan — Little Sylly Games Studio Audit
**Generated by:** Fable audit — June 2026 (running log; finalised in Phase 6)
**Status:** Audit in progress — entries accumulate per phase
**Total issues:** 53 (0 critical, 19 bugs, 24 polish, 10 doc)

> Work through CRITICAL items first. Each entry has a self-contained Fix line —
> no cross-referencing required. Mark items complete by changing `###` to `### ✅`.
>
> ⚠️ index.html rule: never apply systematic/bulk edits to index.html with an
> editor's find-replace or an AI Edit tool — use a Node.js script (UTF-8 mojibake
> risk; established project rule). One-off single-element edits are fine.
>
> After completing all fixes that touch precached assets: bump the SW version once
> (not per-fix) and update CLAUDE.md + logic-engine.md to match.

---

## [BUG] items

### [BUG] Engine — 20 overlays are never hidden by any reset path

**Where:** `js/engine.js` — `resetToLobby()` (and the per-game reset functions it delegates to: `resetSyllySignals`, `resetSecretMode`, `resetYouGetIt`, `resetLateToTheParty`, `natResetState`, `dsdResetState`, `bldResetState`, `gthResetState`)
**What:** These overlays are hidden only by their own close/cancel buttons — no reset path touches them: `settings-overlay`, `deck-panel`, `skip-turn-overlay` (LI5); `gm-settings-overlay`, `gm-near-sync-overlay`, `gm-boost-overlay`, `gm-neural-library-overlay`, `gm-new-frequency-overlay`, `gm-vocab-overlay`, `gm-concede-overlay`, `gm-deck-panel`, `gm-how-to-overlay` (GM); `ss-settings-overlay`, `ss-dossier-overlay`, `ss-how-to-overlay` (SS); `ygi-help-tip-overlay` (YGI); `lttp-smalltalk-overlay`, `lttp-tip-overlay`, `lttp-help-tip-overlay` (LTTP); `sound-overlay` (global). Verified by scripted token-exact sweep of `resetToLobby()` + all delegated reset bodies (13 June 2026); `ygi-help-tip-overlay` + the two LTTP tip overlays added by the Phase 3 YGI/LTTP read (the original scripted sweep matched only `lttp-smalltalk-overlay` for LTTP).
**Why it matters:** In Lobby Mode, `resetToLobby()` can fire while any overlay is open (host taps end-game, or client confirms the host-disconnected overlay) — the stale overlay then survives on top of the lobby, the DYB BUG-05 ghost-interceptor class. LI5, GM, SS, and LTTP are all multiplayer-capable, so every listed overlay is reachable in this window.
**Fix:** Add a `style.display = 'none'` for each listed overlay to `resetToLobby()` (or to the owning game's delegated reset function); `sound-overlay` can be added to the engine's own teardown block.
**Verify:** Re-run the sweep: every `fixed inset-0` overlay id in `index.html` appears (token-exact or via template-literal loop) in `resetToLobby()` + delegate bodies; manually open GM settings on a client device, kill the host, confirm OK → lobby shows with no stale overlay.
**Rule violated:** `phase-audit.md` Protocol C Part 2 (`resetToLobby()` teardown row); DYB BUG-05 lesson

### [BUG] BLD — lobby has no `getMinPlayers` at all; engine default allows a 2-player start (role table starts at 5)

**Where:** `js/engine-multiplayer.js` — `MP_GAME_CONFIGS.bld` (lines ~206–212) and the fallback `mpActiveGameConfig?.getMinPlayers?.() ?? 2` (line ~1471)
**What:** The BLD config defines `getMaxPlayers: () => 10` but no `getMinPlayers`, so the lobby falls back to the engine default of **2**. `BLD_ROLE_TABLE` and `BLD_GROUP_TABLE` in `bld.js` have no row below 5 (the game is designed for 5–10; `game-identities.md` documents min 5). (Phase 2 of this audit logged this as `getMinPlayers: () => 4` — that was a misread of GTH's neighbouring entry; reality is the field is absent entirely.)
**Why it matters:** A 2–4 player start looks up `BLD_ROLE_TABLE[count]` → `undefined`, crashing role assignment or silently producing a broken game for all devices.
**Fix:** Add `getMinPlayers: () => 5` to the BLD entry of `MP_GAME_CONFIGS`.
**Verify:** Host a BLD lobby with 4 joined players — the start CTA must stay disabled; with 5 it enables.
**Rule violated:** `game-identities.md` § Game 11 Bailed (Min players: 5)

### [BUG] PASS — `MP_GAME_CONFIGS.pass` missing display fields; mode screen renders "undefined"

**Where:** `js/engine-multiplayer.js` — `MP_GAME_CONFIGS.pass` (lines ~257–275) and `mpShowModeScreen()` (lines ~400–405)
**What:** The PASS config has a stray `gameId: 'pass'` key (read by nothing) and is missing the four display fields every other game defines: `gameName`, `emoji`, `ptpLabel`, `brandBtnClass`. `mpShowModeScreen()` unconditionally writes all four into the mode screen — and PASS's menu Play CTA does route through `mpShowModeScreen('pass')` (`pass.js` line ~1289).
**Why it matters:** Every PASS multiplayer entry shows a mode screen with "undefined" as the emoji, "undefined" as the game name, a CTA labelled "undefined", and a CTA `className` containing the literal string `undefined` (no background colour — white text on white).
**Fix:** Add `gameName: 'Pass'`, `emoji: '🃏'`, `ptpLabel: 'Deal Me In'`, `brandBtnClass: 'bg-zinc-900 hover:bg-zinc-800'` to the PASS entry and delete the stray `gameId` key.
**Verify:** Tap Pass → Deal Me In from the lobby: mode screen shows 🃏 / "Pass" and a black branded CTA reading "Deal Me In".
**Rule violated:** `ui-style.md` § Per-game brand reference (PASS row); `logic-engine.md` § Multiplayer checklist (MP_GAME_CONFIGS entry)

### [BUG] LI5 — Toy Box deck panel renders behind the settings overlay (untappable)

**Where:** `index.html` — `#deck-panel` (`z-[60]`) vs `#settings-overlay` (`z-[80]`)
**What:** "Edit Toy Box ▸" (inside the settings overlay, which stays open) shows `deck-panel` at `z-[60]` — two layers *below* its opener. Both are 80vh bottom sheets, so the deck panel is fully hidden behind the settings sheet; tapping the button appears to do nothing and categories cannot be customised. GM's equivalent `gm-deck-panel` is correctly `z-[100]`.
**Why it matters:** The category picker is unreachable — the OFF state of The Toy Box setting is unusable (players can toggle OFF but never choose decks, then `startGame()` silently refuses to start if the set were empty).
**Fix:** Change `z-[60]` to `z-[100]` on `#deck-panel` (one-off single-element edit).
**Verify:** Settings → toggle The Toy Box OFF → tap "Edit Toy Box ▸" → category grid appears above the settings sheet and is tappable.
**Rule violated:** `ui-style.md` § Z-Index Stack
**Logged:** `li5-implementation-notes.md` L6

### [BUG] LI5 — cancelling a quit from the gatekeeper screen starts a phantom turn timer

**Where:** `js/games/li5.js` — `hideQuitConfirm()` / `btn-gatekeeper-exit` listener
**What:** `btn-gatekeeper-exit` opens `quit-overlay`; the shared `btn-quit-cancel` handler calls `hideQuitConfirm()`, which unconditionally restarts the turn timer (`if (!isPaused) startTimer()`) even though no turn is running on the gatekeeper. The timer ticks audibly in the background; if `timeLeft` reaches 0 it fires `endTurn()` → re-shows the previous turn's stale Report Card → confirming it double-advances the team rotation.
**Why it matters:** Phantom tick sounds at minimum; a skipped team turn and duplicated match-log entries at worst.
**Fix:** Restart the timer only when the quit overlay was opened from `screen-active-play` — e.g. record the opener in `showQuitConfirm()`/the gatekeeper listener, or guard on the visible screen before `startTimer()`.
**Verify:** Gatekeeper → ✕ → "Keep going!" → no tick sounds, no timer running (wait >90s to confirm no spontaneous Report Card).
**Rule violated:** — (logic bug)
**Logged:** `li5-implementation-notes.md` L7

### [BUG] GM — Lobby Mode near-sync round is silently discarded

**Where:** `js/games/great-minds.js` — `gmMpResolveRound()`
**What:** With Resonance Tolerance = Resonant, a near-sync pair (e.g. "dog" / "dogs") takes the `isNearSync` branch, which calls *neither* `gmHandleMatch()` nor `gmHandleMismatch()`: the round is never logged, `gmCurrentPair` / `gmPrevRoundWords` never update, and the host's result screen shows stale previous-round content (blank on round 1). Clients render it as a normal mismatch but with the unchanged pair.
**Why it matters:** The round vanishes from Psychic Echoes, the next round replays the same pair, and host/client screens disagree — in the host's own words in the code, near-sync was meant to be "treated as mismatch for now" but the mismatch handler is never invoked.
**Fix:** In the `isNearSync` case, either call `gmHandleMismatch()` (minimal fix matching the comment's intent) or show the Near-Sync overlay host-side and broadcast the decision (full-feature fix).
**Verify:** MDLM lobby, Resonant tolerance, submit "dog"/"dogs" → both devices show a coherent result, the round appears in Psychic Echoes, and the next round uses the two clues as the new pair.
**Rule violated:** `logic-engine.md` § Interceptor Pattern (host must resolve and broadcast consistent state)
**Logged:** `gm-implementation-notes.md` G4

### [BUG] Suite-wide — `mpLockSync()` is a no-op for all 8 Phase-22 games (`btn-mp-action` never applied)

**Where:** `index.html` (only BLD/GTH/DYB/PASS buttons carry `btn-mp-action`) + `js/engine-multiplayer.js` (`syllySyncLocked` is written at lines 524/533 but read nowhere)
**What:** The sync lock works exclusively through the `.btn-mp-action` CSS class, and none of LI5/GM/SS/JEC/YGI/LTTP/NAT/DSD applies it to any submit button; `window.syllySyncLocked` itself gates nothing. Calling `mpLockSync()` in those games disables no buttons. JEC's prep submit self-protects (manual `pointer-events-none`); the others don't.
**Why it matters:** Worst concrete case is SS: double-tapping "Confirm Code" sends two `SS_DECODE_SUBMIT` ACTIONs and the Host runs `ssResolve()` twice — double token/misfire increments on the authoritative device (wrong game outcome). Other games risk duplicate ACTIONs with milder effects.
**Fix:** Add `btn-mp-action` to every submittable multiplayer button in the eight Phase-22 games (Node script for the bulk `index.html` pass), or make `mpSendEnvelope` drop ACTION sends while `syllySyncLocked` is true (single-point fix; still add the class for the visual grey-out).
**Verify:** In an SS lobby session, double-tap Confirm Code rapidly — Host resolves exactly once; greyed button visible during sync.
**Rule violated:** `logic-engine.md` § Sync Lock + `definitions.md` § `btn-mp-action`
**Logged:** `ss-implementation-notes.md` S10

### [BUG] SS — Lobby Mode client double-applies resolution (inflated scoreboard + duplicated journal entry)

**Where:** `js/engine-multiplayer.js` — `SS_RESOLUTION` SYNC handler (line ~1351)
**What:** The handler assigns the Host's post-increment `tokens`/`misfires`/clue histories/`roundHistory`, then calls `ssResolve()`, which increments tokens/misfires again, pushes a duplicate `ssRoundHistory` entry, and re-archives the clues.
**Why it matters:** The client's resolution screen shows one extra token/misfire whenever one was scored, and the final round appears twice in the client's Mission Journal at gameover. (Self-corrects on the next SYNC; Host state and win decision unaffected.)
**Fix:** Split `ssResolve()` into resolve + render halves; the SYNC handler calls only the render half against the payload state (mirror of `gmMpDisplayResult` pattern).
**Verify:** TLM round with an interception — client resolution scoreboard matches Host exactly; client gameover Mission Journal has no duplicated half.
**Rule violated:** `logic-engine.md` § Interceptor Pattern (clients apply resolved state, never re-resolve)
**Logged:** `ss-implementation-notes.md` S9

### [BUG] SS — multiplayer guards assume exactly 2 devices; MDLM roster permits up to `ssPlayerCount × 2`

**Where:** `js/games/secret-signals.js` (vault-done, intercept/decode submit guards using `mpMyPlayerIdx` as a team index) + `js/engine-multiplayer.js` (`SS_VAULT_DATA` broadcast, `MP_GAME_CONFIGS.ss.getMaxPlayers`)
**What:** Every SS lobby-mode guard treats `mpMyPlayerIdx` as a team index (0/1). With more than 2 devices: devices idx ≥ 2 can never submit anything (`ssMpVaultReady[2]` etc.), and the broadcast `SS_VAULT_DATA` SYNC delivers Team B's vault to every client — including extra Team A devices (the docs' "targeted write" claim was wrong; corrected June 2026). `mpSerialiseSettings('ss')` also omits `ssSelectedCategories`, and client vault rerolls send no ACTION (Host's copy of Team B's vault goes stale).
**Why it matters:** An MDLM SS lobby with 3+ devices soft-locks at the vault stage and leaks vault keywords across teams.
**Fix:** Either cap SS lobbies at 2 devices (`getMaxPlayers: () => 2`, drop `'mdlm'` or treat it as TLM-equivalent) — the minimal honest fix — or add device→team translation to every guard plus a targeted vault write.
**Verify:** SS MDLM lobby with 3 devices either cannot start (cap) or all devices can play their team's turns with no cross-team vault exposure.
**Rule violated:** `logic-engine.md` § MDLM missing-handler audit; `game-identities.md` § SS Multiplayer (corrected)
**Logged:** `ss-implementation-notes.md` S11

### [BUG] SS — Intel Phase (Sylly Mode) unsupported in Lobby Mode; client device crashes on Team B's turn

**Where:** `js/games/secret-signals.js` — Intel Phase functions (no MP branches) + `ss-endgame-splash` Phase 2 button
**What:** Phase 2 has zero MP packets. After `SS_ENDGAME` both devices show the Phase 2 button; each runs the pass-the-phone Intel Phase locally. The client never receives Team A's vault (`ssVaultA` is `[]`), so `ssIntelTargetVault()[idx].word` throws the moment Team B's guessing turn starts on the client.
**Why it matters:** Multiplayer + Sylly Mode = client-side crash mid-endgame; even before the crash, the two devices run divergent intel phases.
**Fix:** Short-term: force `ssIntelSyllyMode = false` when `syllyMultiplayerMode !== 'single'` (and hide the toggle's effect in lobby sessions). Long-term: host-device-only intel with a client standby screen, or full packet support.
**Verify:** TLM game with Sylly Mode ON reaches endgame — client either never sees the Phase 2 button or shows a standby; no console error.
**Rule violated:** `logic-engine.md` § Missing handler audit
**Logged:** `ss-implementation-notes.md` S12

### [BUG] JEC — client-side Sous Chef merges are unguarded in Lobby Mode

**Where:** `js/games/jec.js` — `jecHandleOversightTap()` / `jecApplyMerge()`
**What:** Oversight tap handlers are wired on every device when the setting is on; `jecApplyMerge()` only adds a broadcast when host. A client merging two cards mutates its local frequency/display/poison state with no ACTION sent — the impl-notes claim "clients cannot initiate merges" is documentation, not code.
**Why it matters:** The client's sifting view silently diverges from the table; scores stay correct (Host computes them) but players argue over different boards.
**Fix:** In `jecHandleOversightTap()`, early-return when `window.syllyMultiplayerMode === 'client'` (and hide the oversight hint on clients).
**Verify:** MDLM client taps two sift cards — nothing happens; host merge still propagates via `JEC_MERGE`.
**Rule violated:** `logic-engine.md` § Interceptor Pattern
**Logged:** `jec-implementation-notes.md` J3

### [BUG] JEC — sifting/tally CTAs not host-gated; clients can run divergent rounds

**Where:** `js/games/jec.js` — `btn-jec-sifting-proceed` / `btn-jec-tally-next` listeners; `js/engine-multiplayer.js` — `JEC_NEXT_ROUND` handler
**What:** Neither CTA checks for client mode. A client tapping "The Taste Test!" runs `jecCalcRoundScores()` locally (mutating `jecScores`, duplicating a `jecRoundLog` entry) and self-navigates; "Next Course" runs `jecStartRound()` locally, popping the client's own word pool and flashing a wrong order word until `JEC_ORDER` lands. The `JEC_NEXT_ROUND` handler causes the same transient even on well-behaved clients (full `jecStartRound()` instead of waiting for `JEC_ORDER`).
**Why it matters:** A client can run ahead of the table with self-computed (and double-logged) scores; every round start briefly shows the wrong word on clients.
**Fix:** Gate both CTAs host-only (clients show a disabled "Waiting for the Head Chef…" label — GTH host-gate pattern), and reduce the `JEC_NEXT_ROUND` handler to a no-op/waiting state, letting `JEC_ORDER` drive the client's round start.
**Verify:** MDLM client taps Taste Test / Next Course — nothing happens; round advances only on host tap; no wrong-word flash on clients.
**Rule violated:** `logic-engine.md` § Host-gate screens before timed phases (pattern); § Interceptor Pattern
**Logged:** `jec-implementation-notes.md` J4

### [BUG] YGI — Lobby Mode double-pushes the round log; malformed entry crashes the gameover carousel

**Where:** `js/games/ygi.js` — vote-resolution host path (line ~561) + `ygiShowResults()` (line ~694); `js/engine-multiplayer.js` — `YGI_VOTE_SUBMIT` host path (line ~965) + `YGI_VERDICT` client handler (line ~980)
**What:** In MP, the host pushes a round-log entry during vote resolution *and then* calls `ygiShowResults()`, which pushes its own entry — so `ygiRoundLog` gets two entries per round. The manual push is also malformed: `prompt` is the prompt **object** (not `.text`) and `entries` lacks the `name/pts/isGhost/isWinner` fields. The client sets `ygiRoundLog = p.roundLog` then also calls `ygiShowResults()` (another push). The single-device path is correct (`ygiComputeAndShowResults()` does not push; only `ygiShowResults()` does).
**Why it matters:** The gameover carousel indicator shows a doubled count; navigating "prev" lands on a malformed entry where `ygiRenderRoundLog()` calls `entry.prompt.replace('[ ]', …)` on an object → **TypeError** that kills the carousel. MP-only.
**Fix:** Remove the manual `ygiRoundLog.push()` from both host paths; let `ygiShowResults()` own the push and build the SYNC `roundLog` after calling it. On the client, assign `p.roundLog` and skip the local push (or ignore the payload and push locally) — never both.
**Verify:** Play a 3-round MDLM YGI game → gameover carousel reads "1 / 3" (not "1 / 6") and "prev"/"next" navigate all three rounds without a console error.
**Rule violated:** `logic-engine.md` § Interceptor Pattern (clients apply resolved state, never re-derive)
**Logged:** `ygi-implementation-notes.md` Y3

### [BUG] YGI — Sudden Death (Solo Take tie-break) is not multiplayer-aware

**Where:** `js/games/ygi.js` — `ygiStartSuddenDeath()` / `ygiShowSDPassGate()` / `ygiShowSDInput()` / `ygiResolveSuddenDeath()` (no MP branches)
**What:** When `ygiDecider === 'only-one'` (Solo Take) and the final standings tie, the game runs a pass-the-phone sudden-death flow with zero ACTION/SYNC packets. In MDLM each device independently reaches `ygiShowGameOver()` (results-next is not host-gated) and runs its own local SD.
**Why it matters:** Conditional (Solo Take + a tie), but in MDLM the sudden-death is unplayable/divergent — same class as SS Intel-Phase S12.
**Fix:** Short-term: force `ygiDecider = 'close-enough'` (Split Take) when `syllyMultiplayerMode !== 'single'`. Long-term: host-driven SD with ACTION submissions + a `YGI_SUDDEN_DEATH` SYNC.
**Verify:** MDLM YGI with Solo Take, force a points tie → no divergent/locked SD; either Split Take is enforced or all devices follow one host-driven SD.
**Rule violated:** `logic-engine.md` § Missing handler audit
**Logged:** `ygi-implementation-notes.md` Y4

### [BUG] YGI — `btn-ygi-results-next` not host-gated; clients advance the round locally

**Where:** `js/games/ygi.js` — `btn-ygi-results-next` listener (line ~711) → `ygiStartRound()`
**What:** The results "Next Situation →" button runs `ygiStartRound()` on whichever device taps it. On a client this pops the client's own independently-shuffled `ygiPromptPool` and shows `screen-ygi-prompt` with the wrong situation until the host's `YGI_ROUND_START` SYNC overwrites it.
**Why it matters:** Transient wrong-situation flash on clients + divergent `ygiRound` increments (self-corrects on the next SYNC). Low severity — same client-gating-gap class as JEC J4.
**Fix:** Branch the handler on `syllyMultiplayerMode`: clients show a disabled "Waiting for the host…" state; only the host calls `ygiStartRound()`/`ygiShowGameOver()`. Let `YGI_ROUND_START` drive the client's round start.
**Verify:** MDLM client taps Next Situation — nothing happens; round advances only on host tap; no wrong-prompt flash.
**Rule violated:** `logic-engine.md` § Interceptor Pattern
**Logged:** `ygi-implementation-notes.md` Y5

### [BUG] LTTP — MDLM plan narrowing / advancement is never synced (devices diverge after lap 1)

**Where:** `js/engine-multiplayer.js` — `LTTP_MESSAGE_SEND` host handler (line ~1415); `js/games/lttp.js` — `lttpNarrowHighlights()` (line ~995), `lttpSelectPlayer()` lap-completion branch (lines ~960–989)
**What:** Only the messaging loop has packets. The host's `LTTP_MESSAGE_SEND` handler pushes history + advances `lttpActiveIdx` + broadcasts `LTTP_MESSAGE_INTERRUPT`, but never runs the lap-completion logic — so when a *client* completes a lap the host never narrows highlights or advances `lttpPlan`. The lap-completing client narrows locally but its plan-update screen is immediately clobbered by the interrupt-dismiss → `lttpShowChat()`. And `lttpNarrowHighlights()` picks decoys via `shuffle()`, so independent per-device narrowing would diverge anyway. There is no `LTTP_PLAN_UPDATE` SYNC.
**Why it matters:** After the first full lap, devices show different remaining locations and the plan can stall — LTTP's primary multiplayer mode is partially broken mid-game. (Confirm in browser; Phase 28 MDLM testing likely only exercised the first lap's messaging.)
**Fix:** Make the host own lap completion: host computes narrowing once and broadcasts the resulting `lttpHighlights`/`lttpFadedCells`/`lttpPlan` as an `LTTP_PLAN_UPDATE` SYNC (send the resolved sets, not a shuffle seed). Clients apply and render; no client-side narrowing.
**Verify:** MDLM LTTP, complete a full lap with a *client* as the last asker → all devices advance to the same Plan and show identical highlighted cells.
**Rule violated:** `logic-engine.md` § Interceptor Pattern (host resolves and broadcasts consistent state)
**Logged:** `lttp-implementation-notes.md` L5

### [BUG] LTTP — MDLM guess phase + gameover have no MP packets

**Where:** `js/games/lttp.js` — `lttpStartGuessPhase()`, `lttpShowGuess()`/pin+vote screens, `lttpComputeAndShowGameover()` (no MP branches)
**What:** The entire Plan-4 endgame (pin/vote/scoring/reveal) carries zero ACTION/SYNC. In MDLM it either never starts on client devices (when the host completes the final lap) or runs a divergent local pass-the-phone flow.
**Why it matters:** The game cannot be completed correctly in its primary multiplayer mode — exact mirror of the SS Intel-Phase gap (S12).
**Fix:** Host-driven endgame: clients submit pins/votes via ACTION; host resolves and broadcasts `LTTP_GAMEOVER` with winner + reason + all votes. (Or document MDLM as messaging-only and hard-gate the endgame to the host.)
**Verify:** MDLM LTTP reaches Plan 4 → all devices enter the guess phase, submit, and land on the same gameover result with no console error.
**Rule violated:** `logic-engine.md` § Missing handler audit
**Logged:** `lttp-implementation-notes.md` L6

### [BUG] NAT — Selection voting + Last Stand are not MP-distributed (every device runs its own local vote)

**Where:** `js/games/nat.js` — `natStartVoting()`, `btn-nat-sel-start` / `btn-nat-sel-consensus-submit` listeners, `natSubmitVote()`, `natSubmitMoleGuess()`, `natBiologistVerdict()`; `js/engine-multiplayer.js` — `NAT_SELECTION` / `NAT_TALLY` handlers (lines ~1069–1084)
**What:** The observation loop is fully synced and the clue reveal (`NAT_SELECTION`) + final scores (`NAT_TALLY`) are broadcast, but the voting phase between them carries no packets and no host gate. Tapping "Begin Classification" (`btn-nat-sel-start` → `natStartVoting`) on any device runs the vote locally and applies the peer-review −5 deductions locally; the consensus/independent vote controls, the mole guess, and the Biologist verdict are equally ungated. `natMpVoteReadyCheck` is reset in both the `NAT_MATCH_START` and `NAT_SELECTION` handlers but is read nowhere — the intended per-device vote collection was scaffolded and never built.
**Why it matters:** In MDLM each device runs a divergent local vote → eviction → Last Stand, and the single-device pass-the-phone independent-vote flow expects the *holding* device to enter votes for every (remote) player — unplayable as designed. The host's `NAT_TALLY` makes the end scores converge, but the vote experience is broken. Same class as SS Intel-Phase (S12), YGI Sudden Death (Y4), LTTP guess/gameover (L6).
**Fix:** Short-term: hard-gate the Selection→Last-Stand flow to the host (clients show a "Waiting for the field team…" standby after `NAT_SELECTION`). Long-term: per-device vote collection — each device submits its own player's vote via a `NAT_VOTE` ACTION, host aggregates against `natMpVoteReadyCheck.every(Boolean)`, then drives eviction/Last-Stand with ACTION submissions from the actual Mole/Biologist devices.
**Verify:** MDLM NAT reaches the Selection → all client devices either show a host-driven standby or submit exactly one vote each; eviction, mole guess, and verdict happen once (host-authoritative) and all devices land on the same tally.
**Rule violated:** `logic-engine.md` § MDLM missing-handler audit
**Logged:** `nat-implementation-notes.md` N2

### [BUG] DSD — Nuclear Mine ends the game without broadcasting `DSD_GAMEOVER` (spectator stranded)

**Where:** `js/games/dsd.js` — `dsdResolveHit()` Nuclear Mine branch (line ~912, `setTimeout(() => dsdShowGameover(), 2600)`) vs `dsdAdvanceTurn()` (line ~959, the only `DSD_GAMEOVER` broadcaster)
**What:** A Nuclear Mine detonation is terminal inside `dsdResolveHit` and jumps straight to `dsdShowGameover()` after a 2.6s delay, bypassing `dsdAdvanceTurn()` → `dsdCheckVictory()`. `dsdShowExecution()` still broadcasts `DSD_EXECUTION_RESULT` (grid + −1000 Valour), but no path broadcasts `DSD_GAMEOVER`. Danger Level is serialised (`mpSerialiseSettings('dsd')`), so a Lobby Mode game can reach this state.
**Why it matters:** The active crew device shows gameover via its own local `setTimeout` (host, or client execution preview), masking the gap there — but the non-active **spectator (TLM) / standby (MDLM)** device only receives `DSD_EXECUTION_RESULT` and is stranded on the spectator/execution screen, never reaching the gameover screen. Normal victory (via `dsdAdvanceTurn`) is unaffected.
**Fix:** In the Nuclear Mine branch, when `window.syllyMultiplayerMode === 'host'`, broadcast the `DSD_GAMEOVER` SYNC (valour + grid + turnLog) alongside scheduling the local `dsdShowGameover()` — mirror the broadcast `dsdAdvanceTurn()` already does for normal victory.
**Verify:** TLM DSD with Danger Level = Nuclear Mine; active crew arms a Nuclear Mine → both the active device and the spectator device advance to the gameover screen.
**Rule violated:** `logic-engine.md` § Interceptor Pattern (host must broadcast every terminal state)
**Logged:** `dsd-implementation-notes.md` Bug Index (Nuclear Mine gameover)

## [POLISH] items

### [POLISH] SS — settings overlay is legacy format (bare divs + `<hr>`, centred title)

**Where:** `index.html` — `#ss-settings-overlay` ("Operations Briefing 🔐")
**What:** Settings are bare divs separated by `<hr>` with a centred `h3` title — predates the Settings Card Standard (white cards, no separators) and the left-aligned sticky title block. Same legacy class as LI5's settings overlay.
**Fix:** Restructure to white setting cards + standard title block (Node script — multi-element `index.html` edit). Keep the working `.overflow-y-auto` scroll-reset behaviour in `ssOpenSettings()` consistent with whatever structure results.
**Verify:** SS settings visually matches JEC/NAT card format; scroll reset on open still works.
**Rule violated:** `ui-style.md` § Settings Card Standard / § Settings Layout Standard
**Logged:** `ss-implementation-notes.md` S13

### [POLISH] SS — "Game Over" generic eyebrow on the gameover screen

**Where:** `index.html` — `#screen-ss-gameover` eyebrow `<p>` ("Game Over")
**What:** Legacy generic string on an otherwise mission-voiced screen (Protocol A §3 grep target).
**Fix:** Replace with a game-voiced label, e.g. "Mission Complete" / "Mission Debrief" (one-off edit).
**Verify:** Gameover screen shows no bare "Game Over".
**Rule violated:** `phase-audit.md` Protocol A §3 (legacy generic strings)

### [POLISH] SS — `btn-ss-customise-toggle` missing `shrink-0` in initial HTML

**Where:** `index.html` — `#btn-ss-customise-toggle` (`class="sylly-toggle-off"`)
**What:** Initial OFF class lacks `shrink-0`; JS-applied states include it. (Same finding class as GM's `btn-gm-custom-toggle`.)
**Fix:** Add `shrink-0` to the initial class attribute (one-off edit).
**Verify:** Toggle never compresses when the card title wraps.
**Rule violated:** `ui-style.md` § Settings Card Standard (`shrink-0` rule)

### [POLISH] SS — no header `[?]` → How to Play on gameplay screens

**Where:** `index.html` — SS encrypt/intercept/decode screen headers
**What:** The encrypt header's `?` is a contextual tip (`btn-ss-encrypt-tip`); no SS gameplay screen offers the How to Play overlay mid-game. The rule requires a header `[?]` wired to `[abbr]-how-to-overlay` on the main gameplay screen.
**Fix:** Add a `[?]` how-to button to the encrypt screen header (left of the speaker), keeping the contextual tips inline.
**Verify:** Mid-game `[?]` opens "How to Play 📡".
**Rule violated:** `ui-style.md` § Help icon `[?]`; `phase-audit.md` Protocol A §4
**Logged:** `ss-implementation-notes.md` S16

### [POLISH] SS — round numbering lopsided when Team B encrypts first

**Where:** `js/games/secret-signals.js` — `ssNextHalf()` (round increments only after Team 1's half)
**What:** If `showWhoFirst()` picks Team B, "Round 1" contains only Team B's broadcast and all later rounds pair A-then-B. Alternation and scoring are unaffected; the Mission Journal grouping is cosmetically uneven.
**Fix:** Track halves-played instead (`ssRound = Math.floor(halves / 2)`) or increment after the *second* team of each round regardless of index.
**Verify:** Team B first → Mission Journal shows two halves in every round.
**Rule violated:** — (display consistency)
**Logged:** `ss-implementation-notes.md` S17

### [POLISH] JEC — how-to overlay states the wrong Rotten penalty (−10 vs actual −5)

**Where:** `index.html` — `#jec-how-to-overlay` "Winning and Scoring" card
**What:** Card says "A Bit Pongy — (penalty on: −10 pts)"; the code and settings copy agree on −5. The Too Many Cooks line's "−2 per extra Chef" is also imprecise (it's −2 per Chef who picked it, including you).
**Fix:** Update both lines to −5 pts and "−2 per Chef who picked it" (one-off edits).
**Verify:** How-to numbers match `jecCalcRoundScores()` and the settings-card copy.
**Rule violated:** Audit Principle 3 (reality wins) — post-Phase-29 copy sweep miss
**Logged:** `jec-implementation-notes.md` J2

### [POLISH] JEC — New Shift overlay is z-[80]; play-again confirms are z-[90]

**Where:** `index.html` — `#jec-new-shift-overlay`
**What:** The play-again confirmation modal sits at z-[80]; the standard for play-again overlays is z-[90] (quit/settings own z-[80]).
**Fix:** Change `z-[80]` → `z-[90]` (one-off edit).
**Verify:** Overlay stacks above any z-[80] element.
**Rule violated:** `logic-engine.md` § Play-Again Confirmation (z-[90])

### [POLISH] LI5 — play-again modal inner uses non-canonical class string

**Where:** `index.html` — `#li5-play-again-overlay` inner div (`bg-white rounded-3xl p-6`)
**What:** The decision-modal inner uses `bg-white` and `p-6` instead of the canonical `bg-stone-50 … rounded-3xl px-6 pt-6 pb-8` string (border is present and correct).
**Why it matters:** Visual drift from every other decision modal in the suite.
**Fix:** Replace the inner div classes with the verbatim Pattern 2 string from `ui-style.md` (keep `border-pink-300`).
**Verify:** Overlay matches the quit/skip-turn modals' background and padding.
**Rule violated:** `ui-style.md` § Two-Pattern Overlay Library (exact class strings)

### [POLISH] LI5 — settings overlay uses legacy centred title instead of the standard title block

**Where:** `index.html` — `#settings-overlay` ("Learning Plan 📝")
**What:** The title is a centred `h3` + subtitle inside the padded inner, not the standard left-aligned `px-5 pt-5 pb-4 border-b border-stone-200 flex-shrink-0` title block used by every other game.
**Why it matters:** Only game whose settings sheet lacks the standard sticky title-block treatment.
**Fix:** Restructure the header to the Settings Layout Standard block (single-element edit; keep "Learning Plan 📝" / "Adults make the rules! ✋" copy).
**Verify:** Title block visually matches GM/SS/etc.; scroll reset on open still works.
**Rule violated:** `ui-style.md` § Settings Layout Standard

### [POLISH] LI5 — Pinky Swear flips can desync the Report Card delta from the real score

**Where:** `js/games/li5.js` — `flipEntry()`
**What:** Live play clamps point penalties to the available score; `flipEntry()` recomputes flipped entries as raw `-mult` with no clamp. The team total stays floored at 0 via `Math.max(0, …)`, but the "−N pts this turn" label and per-row values can disagree with the actual score change.
**Why it matters:** Players see a delta that doesn't match the scoreboard when a team is at/near 0.
**Fix:** Apply the same clamp in `flipEntry()` or recompute entry points sequentially from `scoreBeforeTurn`.
**Verify:** Team at 0 points, flip a correct → taboo in the Report Card: delta label matches the unchanged score.
**Rule violated:** — (display consistency)
**Logged:** `li5-implementation-notes.md` L8

### [POLISH] GM — quit overlay border is teal (SS brand), not purple

**Where:** `index.html` — `#gm-quit-overlay` inner div (`border border-teal-300`)
**What:** GM's quit modal uses Secret Signals' teal border; every other GM decision modal uses `border-purple-300`. Likely copy-paste from the adjacent SS quit overlay.
**Fix:** Change `border-teal-300` → `border-purple-300` (one-off edit).
**Verify:** GM quit modal border matches the override/near-sync/new-frequency modals.
**Rule violated:** `ui-style.md` § Two-Pattern Overlay Library (border rule — brand colour)
**Logged:** `gm-implementation-notes.md` G6

### [POLISH] GM — host and clients can show different mismatch phrases in Lobby Mode

**Where:** `js/games/great-minds.js` — `gmMpResolveRound()` + `gmHandleMismatch()`
**What:** The host picks `mismatchPhrase` for the SYNC payload, but `gmHandleMismatch()` then overwrites the host's heading with a second random pick — host and clients can display different phrases for the same round.
**Fix:** Pass the chosen phrase into `gmHandleMismatch()` (optional parameter) or set the heading after the call.
**Verify:** MDLM mismatch round — both devices show the identical phrase.
**Rule violated:** — (sync cosmetics)
**Logged:** `gm-implementation-notes.md` G5

### [POLISH] GM — Frequency Tuner toggle missing `shrink-0` in initial HTML

**Where:** `index.html` — `#btn-gm-custom-toggle` (`class="sylly-toggle-off"`)
**What:** The initial OFF class lacks `shrink-0`; after the first click the JS-applied class strings include it. All other GM toggles have `shrink-0` from the start.
**Fix:** Add `shrink-0` to the initial class attribute.
**Verify:** Toggle never compresses when the card title wraps on a narrow screen.
**Rule violated:** `ui-style.md` § Settings Card Standard (`shrink-0` rule)

### [POLISH] LTTP — orphaned `screen-lttp-smalltalk` section (dead markup)

**Where:** `index.html` — `<section id="screen-lttp-smalltalk">`
**What:** A full `<section>` for a Small Talk screen exists but is never referenced by `lttp.js`, never registered in `allScreens[]`, and never shown — it is a leftover from before the Phase 21a redesign that made Small Talk an overlay (`lttp-smalltalk-overlay`, which is the live element).
**Why it matters:** Dead markup in a single-file SPA misleads future sessions (it contains `lttp-st-*` IDs that look live) and bloats the file.
**Fix:** Delete the entire `screen-lttp-smalltalk` section via a Node script (index.html bulk-edit rule), after confirming none of its child IDs (`lttp-st-ask-*`) are referenced in `lttp.js`.
**Verify:** Grep `lttp.js` for each removed child ID → zero hits; Small Talk Helper still works via `lttp-smalltalk-overlay`.
**Rule violated:** `phase-audit.md` 2B-10 (orphaned markup)

### [POLISH] GTH — Play CTA contains an emoji

**Where:** `index.html` — `#btn-gth-menu-play` ("Start the Session 🛋️")
**What:** The game menu Play CTA includes an emoji, violating the rule "CTA start-game buttons (game menu) must not contain emoji" (`ui-style.md` § Settings Card Standard).
**Why it matters:** Cross-game inconsistency — all other 11 Play CTAs are text-only.
**Fix:** Remove the 🛋️ from the button label (one-off single-element edit — fine without a script).
**Verify:** Open GTH menu; CTA reads "Start the Session" with no emoji.
**Rule violated:** `ui-style.md` § Settings Card Standard (CTA emoji rule)

### [POLISH] DYB — how-to overlay uses muted stone-400 instead of brand stone-700

**Where:** `index.html` — `dyb-how-to-overlay`: step labels `text-stone-400`, close button `bg-stone-400 hover:bg-stone-500`
**What:** Step labels and the close button use stone-400, lighter than DYB's stone-700 brand; every other game uses its full brand colour for both.
**Why it matters:** Visual drift from the How-to Overlay Standard; the close button reads as disabled-grey.
**Fix:** If unintentional, change step labels to `text-stone-700` and close button to `bg-stone-700 hover:bg-stone-800` (Node script if batch-editing; individual edits fine). If a deliberate muted choice, document it in `ui-style.md`'s per-game table note instead.
**Verify:** DYB how-to overlay step labels and close button match the brand table row.
**Rule violated:** `ui-style.md` § How-to Overlay Standard

### [POLISH] GM / LTTP / NAT — doubled `id` attribute on the header `[?]` buttons

**Where:** `index.html` lines ~729 (`btn-gm-how-to`), ~3323 (`btn-lttp-how-to`), ~3889 (`btn-nat-how-to`)
**What:** Each of these three buttons has the same `id` attribute written twice on the one tag (`id="btn-gm-how-to" id="btn-gm-how-to"`). Browsers ignore the duplicate attribute, so behaviour is unaffected — there is no second element.
**Why it matters:** Invalid HTML; a future regex-based sweep (like this audit's) counts them as duplicate IDs and wastes investigation time.
**Fix:** Remove the second `id="..."` from each of the three tags (one-off single-element edits — fine without a script).
**Verify:** Scripted duplicate-id sweep over `index.html` reports zero duplicates.
**Rule violated:** — (markup hygiene)

### [POLISH] css/styles.css — dead `.pill.pill-active` rule (bare `pill-active` never referenced)

**Where:** `css/styles.css` lines ~48–51 — `.pill.pill-active { background: #ec4899; ... }`
**What:** The suffix-less `pill-active` class is defined (pink-500) but referenced nowhere in `index.html` or any JS file — every live usage is `pill-active-[colour]`.
**Why it matters:** Dead code invites accidental reuse; it predates the per-game colour family.
**Fix:** Delete the `.pill.pill-active` rule block.
**Verify:** Grep `index.html` + `js/` for `pill-active"` / `'pill-active'` (bare, no suffix) → zero hits; pill toggles still style correctly.
**Rule violated:** `phase-audit.md` Protocol A §2 (dead/deprecated class)

### [POLISH] index.html — Google Fonts loaded from CDN; brand font is network-dependent

**Where:** `index.html` `<head>` — `<link>` tags to `fonts.googleapis.com` / `fonts.gstatic.com` (Fredoka)
**What:** The Fredoka font is loaded from Google Fonts at runtime and is not (and cannot trivially be) precached by the SW; the project's documented constraint is "local Tailwind, no CDN, fully offline".
**Why it matters:** Offline/installed PWA sessions silently fall back to the system font — the app works but loses its brand typography, and the docs overstate the offline guarantee.
**Fix:** Either self-host Fredoka (download the woff2 files into e.g. `css/fonts/`, add a local `@font-face`, add to `sw.js` precache, bump SW) or document the deliberate exception in `CLAUDE.md` § Tech Stack and `logic-engine.md` § PWA Guardian.
**Verify:** DevTools offline mode → app renders in Fredoka (if self-hosted), or docs name the exception.
**Rule violated:** `CLAUDE.md` § Tech Stack & Zero-Cost Constraints (no CDN / fully offline)

### [POLISH] css/styles.css — dead `.sylly-toggle-on` rule

**Where:** `css/styles.css` line ~70 — `.sylly-toggle-on`
**What:** The deprecated `sylly-toggle-on` class has zero usages in `index.html` or any JS file (verified June 2026) but its CSS rule still exists.
**Why it matters:** Dead code invites accidental reuse of a deprecated pattern.
**Fix:** Delete the `.sylly-toggle-on` rule block (keep `.sylly-toggle-off` — it is a live legacy alias of `.game-toggle-off`).
**Verify:** `Select-String sylly-toggle-on` returns no hits in css/, js/, or index.html.
**Rule violated:** `phase-audit.md` Protocol A §2 (deprecated class)

### [POLISH] YGI — `ygiShowHelpTip()` defined twice

**Where:** `js/games/ygi.js` — lines ~58 and ~153
**What:** The same function is declared twice, identically; the second wins. Dead duplication.
**Fix:** Delete the first definition (lines ~58–63).
**Verify:** Grep `ygi.js` for `function ygiShowHelpTip` → one hit; the input-screen `[?]` tip still opens.
**Rule violated:** — (dead code)
**Logged:** `ygi-implementation-notes.md` Template Gaps

### [POLISH] YGI — settings toggles use the legacy `sylly-toggle-off` OFF-state alias

**Where:** `js/games/ygi.js` — `btn-ygi-full-tally-toggle` (line ~130), `btn-ygi-sylly-toggle` (line ~138)
**What:** OFF state is set to `sylly-toggle-off` rather than the canonical `game-toggle-off`. The alias is still valid (shared CSS rule), so not a bug — but YGI is one of the few games still on the legacy name. (The deprecated class `sylly-toggle-on` is NOT used — Check B clean.)
**Fix:** Swap `sylly-toggle-off` → `game-toggle-off` in both handlers for consistency.
**Verify:** Grep `ygi.js` for `sylly-toggle-off` → zero hits; toggles still render OFF correctly.
**Rule violated:** `ui-style.md` § Mute toggle theming (canonical `game-toggle-off`)

### [POLISH] LTTP — dead `lttpShowRoleReveal()` / `screen-lttp-role-reveal` + implicit-global `lttpRoleRevealIdx`

**Where:** `js/games/lttp.js` — `lttpShowRoleReveal()` (~525), `lttpAssignRoles()` dead `indices` var (~452), `lttpRoleRevealIdx` (assigned ~469/~1504, never declared)
**What:** `lttpShowRoleReveal()` and `screen-lttp-role-reveal` are never reached — roles are shown on the chat screen. `lttpAssignRoles()` has an unused `const indices = shuffle(...)`. `lttpRoleRevealIdx` is assigned but never `let`-declared (implicit global).
**Why it matters:** Dead code + a registered-but-unreachable screen + a leaked global mislead future sessions and bloat the SPA.
**Fix:** Remove the dead function, the unused `indices` line, and `screen-lttp-role-reveal` (Node script for the HTML section) — or wire a real role-reveal gate. If `lttpRoleRevealIdx` is kept, `let`-declare it.
**Verify:** Grep `lttp.js` for `lttpShowRoleReveal` → zero hits; game still reveals roles on the chat screen.
**Rule violated:** — (dead code / markup hygiene)
**Logged:** `lttp-implementation-notes.md` Template Gaps

### [POLISH] LTTP — `lttp-confirm-overlay` inner uses a non-canonical class string

**Where:** `index.html` — `#lttp-confirm-overlay` inner div (`overlay-modal-inner w-full flex flex-col gap-4 border border-red-300`)
**What:** The message-input decision modal omits the canonical `bg-stone-50 max-w-sm rounded-3xl px-6 pt-6 pb-8 text-center` (the border IS present and correct). Same class as LI5's play-again drift.
**Why it matters:** Visual drift from the standard decision-modal shell.
**Fix:** Align to the Pattern 2 class string where compatible with the custom input layout (keep `border-red-300`).
**Verify:** Confirm modal background/padding matches the quit modal.
**Rule violated:** `ui-style.md` § Two-Pattern Overlay Library (exact class strings)

## [DOC] items

### [DOC] index.html — game section header comments stale across ~9 sections

**Where:** `index.html` — the `<!-- ════ ... ════ -->` section header comment blocks
**What:** Scripted diff of header-claimed IDs vs actual markup (13 June 2026) found: all 8 per-game `[abbr]-help-tip-overlay`s (Phase 21a), both contextual tip overlays (`lttp-tip-overlay`, `bld-tip-overlay`), `nat-new-expedition-overlay`, `dyb-slick-picker-overlay`, `ss-dossier-overlay`, `ss-inning-transition`, `ss-endgame-splash`, `lttp-smalltalk-overlay`, `mp-host-prelobby-overlay`, and `screen-mp-roster` are present in the markup but claimed by no header. The LTTP header additionally states "screen-lttp-smalltalk removed Phase 21a" while that section still exists (see the separate orphaned-markup POLISH entry).
**Why it matters:** The headers are the in-file navigation map for a 6000+ line single-page file; stale lists send future sessions to `code-map.md` distrust or wrong conclusions.
**Fix:** Update each header's Screens/Overlays lists to match reality via a Node script (index.html bulk-edit rule). Pair with the `screen-lttp-smalltalk` deletion so the LTTP header note becomes true.
**Verify:** Re-run the header-claims diff script → zero unclaimed screens/overlays.
**Rule violated:** `logic-engine.md` § Checklist (section header comment block); Audit Principle 3

### [DOC] engine-multiplayer.js — `multiplayerOnly` config field is never read by any code

**Where:** `js/engine-multiplayer.js` — `MP_GAME_CONFIGS.*.multiplayerOnly`
**What:** Every config defines `multiplayerOnly`, and `game-identities.md` documents it as if it gates behaviour, but no code reads the field — MDLM-only routing is actually enforced by `supportedModes: ['mdlm']`.
**Why it matters:** A future game could set `multiplayerOnly: true` with a wider `supportedModes` list and expect a gate that doesn't exist.
**Fix:** Either remove the field (and the `multiplayerOnly: true` mentions in game-identities multiplayer sections become prose), or keep it and add a one-line "informational only — enforcement is `supportedModes`" note to `logic-engine.md` § Multiplayer Sync Module.
**Verify:** Grep all JS for `multiplayerOnly` consumption matches the chosen resolution.
**Rule violated:** Audit Principle 3 (reality wins)

### [DOC] BLD — documented `bld-new-night-overlay` does not exist; play-again reuses `bld-second-chances-overlay`

**Where:** `.claude/rules/game-identities.md` § Game 11 Bailed (Overlay Types) vs `index.html` / `js/games/bld.js`
**What:** game-identities lists a `bld-new-night-overlay` ("New Night Out?" play-again confirmation). No such element exists. The shipped play-again flow (`#btn-bld-go-again`) opens `bld-second-chances-overlay` with a dynamic confirm label ("Restart in Lobby 🔄" host / "Leave Session" client / "Second Chances 💬" single) — the same overlay also serves the patience-exhausted Flake-win confirm.
**Why it matters:** A future session would grep for a non-existent overlay; `resetToLobby()` teardown audits would chase a ghost entry.
**Fix:** During Phase 3 BLD audit, update game-identities' Overlay Types table (remove `bld-new-night-overlay`, document the dual-purpose `bld-second-chances-overlay`) — or, if a dedicated New Night Out overlay was the intended design, build it (developer decision; BLD is mid-testing).
**Verify:** game-identities BLD overlay table matches the `bld-*-overlay` IDs actually present in `index.html` (8 overlays).
**Rule violated:** Audit Principle 3 (reality wins)

### ✅ [DOC] SS — no Secret Mode expansion hook function exists — RESOLVED (Phase 3 SS audit, 13 June 2026)

**Where:** `js/games/secret-signals.js`
**Resolution:** Check G traced the hook: SS applies expansion overrides **inline** in `ssConfirmPlayers()` (difficulty, intercepts-to-win, reroll limit, intel mode + customisation lockdown), and `ssBuildVaults()` / `ssRerollWord()` branch on `isSecretMode` to draw from `secretWords`. The hook is functional; only the naming convention (`ssApplyExpansionOverrides()`) differs. Documented in `ss-implementation-notes.md` Design Decisions. Residual note: the Lobby Mode host path bypasses `ssConfirmPlayers()`, so expansion *overrides* (not the word pool) don't apply in multiplayer Secret Mode sessions — folded into the SS multiplayer findings.

### [DOC] GM — brand colour split: violet-500 CTAs vs purple-500 pills/toggles/borders (suite-wide within GM)

**Where:** `index.html` — GM section (Phase 3 audit corrected the earlier "lobby card only" finding)
**What:** The split is much wider than the lobby card: ALL GM primary CTAs and accents are `violet-500/600` (lobby card, menu Play, names-done, start-round, lock-in, gate buttons, settings Done, deck-panel Done, near-sync/override/new-frequency confirms, countdown digits, pass-gate name highlights, win-row text in the echoes log) while `purple-*` is used for pills (`pill-active-purple`), toggles (`game-toggle-on-purple`), the menu Settings tint, all modal borders (`border-purple-300`), and the how-to step labels + close button. `game-identities.md` now records this reality (June 2026); `ui-style.md` tables still say purple-500.
**Why it matters:** Two near-identical hues for one brand; a "unify to purple" edit done from the old docs would have missed ~20 violet elements.
**Fix:** Developer decision: (a) unify — Node script swapping `violet-` → `purple-` across the GM section + `gmRenderInputEchoes`/`gmRenderPsychicEchoes` in `great-minds.js`, or (b) keep the violet-CTA/purple-chrome scheme and document it as deliberate in `ui-style.md`'s per-game table.
**Verify:** Either zero `violet-` classes remain in GM markup/JS, or `ui-style.md` documents the dual scheme.
**Rule violated:** `ui-style.md` § Game Brand Colour — Scope

### [DOC] DYB — Sylly Mode documented as "Chaos Mode"/Slick-only but shipped as "Devil's Luck" with 4+ die types

**Where:** `.claude/rules/game-identities.md` § Game 10 Dicey Bluffs vs `index.html` (DYB settings + how-to overlays)
**What:** `game-identities.md` names DYB's Sylly Mode "Chaos Mode" with only Slick dice; the shipped UI names it "Devil's Luck" and describes Loaded (count double), Phantom (hidden face), Slick (owner-assigned face), and Cracked (count zero) dice.
**Why it matters:** A future session reading game-identities would design or debug against a mechanic that no longer matches the shipped game — high drift risk for multiplayer packets (`allSlickFaces` etc.) too.
**Fix:** During Phase 3 DYB audit, read `dyb.js` in full and rewrite the game-identities Sylly Mode section (terminology, special mechanics, settings card, SYNC payload fields) to match reality.
**Verify:** game-identities DYB entry names "Devil's Luck" and lists every die type implemented in `dyb.js`.
**Rule violated:** Audit Principle 3 (reality wins)

### [DOC] docs/archive — Phase 27 snapshot missing

**Where:** `docs/archive/` — `phase27-snapshot.md`
**What:** CLAUDE.md referenced a Phase 27 snapshot (LI5/GM/SS polish pass) that does not exist anywhere in the repo.
**Why it matters:** A future session following the reference would dead-end; the Phase 27 work (LI5/GM/SS polish) has no implementation record beyond git history.
**Fix:** Either locate/restore the snapshot from an old session, write a brief retroactive snapshot from git history (`9cd8845`/`3d0cee1` era), or accept the gap and rely on the Phase 26/28 snapshots that bracket it. The broken reference has already been removed from CLAUDE.md (audit Phase 1A).
**Verify:** `Test-Path docs/archive/phase27-snapshot.md` returns True, or developer confirms the gap is accepted.
**Rule violated:** `CLAUDE.md` § Documentation Integrity Protocol

### [DOC] docs/archive — Phase 32 (PASS ship) snapshot missing

**Where:** `docs/archive/` — no `phase32-snapshot.md`
**What:** PASS shipped as Phase 32 but no phase snapshot was written; the archive ends at `phase31-snapshot.md`.
**Why it matters:** The Documentation Integrity Protocol requires a snapshot per shipped game; PASS has no implementation record beyond `docs/new-game-tech-pass.md` and impl notes.
**Fix:** Write `docs/archive/phase32-snapshot.md` covering the PASS ship (can be brief — settings, screens, multiplayer packets are already in game-identities.md).
**Verify:** File exists and is referenced from CLAUDE.md Key references.
**Rule violated:** `CLAUDE.md` § Documentation Integrity Protocol

### [DOC] Legacy plugins wire all event listeners at top-level script execution — rule/reality conflict

**Where:** `js/games/li5.js`, `js/games/great-minds.js`, `js/games/secret-signals.js`, `js/games/jec.js`, `js/games/ygi.js`, `js/games/dsd.js` (top-level — non-compliant). **`js/games/lttp.js` and `js/games/nat.js` are the exceptions — they wrap their listeners in `DOMContentLoaded` (compliant).**
**What:** Protocol A §2 requires "no global-scope event listeners — all `addEventListener` calls must live inside an init or start function". LI5, GM, SS, JEC, YGI, and DSD register their listeners at top-level script execution (DSD wires only its `.btn-open-sound` buttons inside `DOMContentLoaded`; everything else is top-level); LTTP and NAT correctly use `DOMContentLoaded`. The top-level pattern is functionally safe in this SPA (all HTML is parsed before the `<script>` tags run — same guarantee the engine's own top-level `.btn-open-sound` wiring relies on), but it means half the suite permanently fails a written audit gate.
**Why it matters:** An audit rule that the stable, shipped games can never pass either gets ignored (rot) or triggers needless refactors of working legacy code.
**Fix:** Developer decision: relax the Protocol A §2 wording to "top-level listeners are acceptable given the script-after-markup load order; new games should still prefer `DOMContentLoaded`/init wiring", or schedule a deliberate refactor of the legacy plugins. Do not refactor during the audit (Principle 1).
**Verify:** `phase-audit.md` Protocol A §2 wording matches the decided policy.
**Rule violated:** Audit Principle 3 (reality wins) vs `phase-audit.md` Protocol A §2

### [DOC] data/secret_words.json — entry with empty category

**Where:** `data/secret_words.json`
**What:** At least one entry has an empty-string `category` (distinct categories found: Heroes, Items, World, "", Social).
**Why it matters:** Category-filtered pools (e.g. JEC food substitution, SS category picker) silently exclude or mishandle uncategorised entries.
**Fix:** Phase 4D will identify the exact entries; assign each a valid category.
**Verify:** `node -e "const d=require('./data/secret_words.json');console.log(d.filter(w=>!w.category).map(w=>w.id))"` prints `[]`.
**Rule violated:** `definitions.md` § Data Schema
